{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "denon run --inspect --allow-net --allow-read --allow-write --allow-plugin --allow-env --import-map import_map.json ./src/index.ts",
      "desc": "Run deno app"
    }
  }
}