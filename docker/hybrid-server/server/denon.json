{
  "$schema": "https://deno.land/x/denon@2.3.2/schema.json",
  "scripts": {
    "start": {
      "cmd": "denon run --allow-net --allow-read --allow-write --unstable --allow-plugin --allow-env --importmap import_map.json ./src/index.ts",
      "desc": "Run deno app"
    }
  }
}